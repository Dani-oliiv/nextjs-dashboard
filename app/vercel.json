{
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/node",
        "config": {
          "installCommand": "corepack enable && pnpm install",
          "buildCommand": "pnpm run build"
        }
      }
    ]
  }